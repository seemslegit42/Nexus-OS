// src/app/account/billing/page.tsx
'use client';
import NexOSBillingPage from '@/app/billing/page';

export default function AccountBillingPage() {
  // The /app/billing/page.tsx is already a comprehensive dashboard-style page
  // that simulates data. We will reuse it here as it's suitable.
  // No further "unmocking" is needed for this specific file if billing/page.tsx is considered "real enough" for now.
  return <NexOSBillingPage />;
}
